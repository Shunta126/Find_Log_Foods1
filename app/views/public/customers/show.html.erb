<div class="row justify-content-center">
  <h4><%= @customer.name %>さんのページ</h4>
</div>

  <div class="row justify-content-center">
    <% if @customer.image.attached? %>
      <%= image_tag customer.image, size: "200x200" %>
    <% else %>
      <%= image_tag 'no_image.jpg', size: "200x200" %>
    <% end %>
  </div>

